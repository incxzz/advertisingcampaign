create table campaign(id identity primary key, name varchar(255) not null, status integer, start_date timestamp, end_date  timestamp);
create table advertisement(id identity primary key, name varchar(255) not null, status integer, private List<Integer> platforms;
  private String assetUrl;
